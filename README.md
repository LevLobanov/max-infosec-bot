# 🛡️ CyberGuard Bot

**CyberGuard Bot** — это многофункциональный помощник по кибербезопасности, разработанный для интеграции в мессенджеры. Он помогает пользователям защититься от фишинга, вирусов и утечек личных данных, предоставляя мгновенную проверку ссылок, файлов, анализ сообщений на мошенничество и поиск данных в агрегаторах утечек.

---

## ✨ Основной Функционал

Бот предоставляет три ключевых инструмента для обеспечения цифровой гигиены:

1.  **Проверка Файлов и Ссылок (VirusTotal):**
    * Пользователь отправляет боту любую подозрительную ссылку или файл.
    * Бот мгновенно отправляет контент на анализ с помощью сервиса **VirusTotal**, предоставляя быстрый и полный отчет об обнаруженных угрозах (вирусы, трояны, фишинг).
2.  **Агрегатор Утечек (LeakLookup):**
    * Пользователь может проверить свои личные данные (email, телефон, пароль, логин) на предмет наличия в общедоступных базах данных, скомпрометированных в результате утечек.
    * Бот сообщает, какие сервисы и когда были скомпрометированы.
3.  **Анализ Сообщений на Мошенничество (AI-Analyzer):**
    * Бот использует продвинутый AI-анализатор для оценки текста сообщений на предмет мошеннических мотивов и приемов социальной инженерии (например, сообщения "от банка", "от родственника в беде").

---

## ⚙️ Сценарии Работы (Как пользоваться)

### 1. Проверка ссылок и файлов (Вирусы и Фишинг)

1.  Пользователь отправляет боту в личном чате или группе **любую ссылку** или **файл** (документ, архив и т.д.).
2.  Бот отвечает: "🔗 Получил вашу ссылку/файл. Быстро проверяю..."
3.  Через несколько секунд бот выводит **сводный результат** сканирования с вердиктом ("Безопасно" или "Опасно") и предлагает кнопку "Полный отчет".

### 2. Проверка данных на утечки

1.  Пользователь нажимает кнопку **"Агрегатор утечек 🗝️"** в стартовом меню.
2.  Бот просит прислать данные: `Пришлите мне то, что нужно проверить: адрес почты, номер телефона, логин или даже пароль.`
3.  Пользователь отправляет данные (например, `myemail@gmail.com`).
4.  Бот ищет совпадения и выводит результат:
    * **Утечек не найдено:** `🥳 Отличные новости! Ваши данные НЕ найдены...`
    * **Утечки найдены:** `🚨 ВНИМАНИЕ! Найдено 2 утечек, связанных с вашими данными! ... (с указанием сервисов и даты)` с рекомендацией немедленно сменить пароли.

### 3. Анализ сообщения на мошенничество

1.  Пользователь нажимает кнопку **"Анализ сообщения 🕵️"** в стартовом меню.
2.  Бот входит в режим сбора данных.
3.  Пользователь пересылает или вводит подозрительное сообщение.
4.  Бот анализирует текст и предоставляет вердикт о наличии мошеннических признаков.

---

## 🚀 Запуск Проекта с помощью Docker Compose

Проект запускается с помощью Docker Compose, что обеспечивает легкую и быструю установку всех необходимых компонентов.

### Шаг 1: Подготовка `.env` файла

Создайте файл `.env` в корневой директории проекта и заполните его необходимыми API ключами.

```env
MAX_BOT_TOKEN = "..."
VIRUSTOTAL_API_TOKEN = "..."
LEAKLOOKUP_PUBLIC_KEY = "..."
AI_TUNNEL_TOKEN = "sk-aitunnel-..."
```

**Описание переменных:**

| Переменная | Описание |
| :--- | :--- |
| `MAX_BOT_TOKEN` | Токен вашего бота в мессенджере. |
| `VIRUSTOTAL_API_TOKEN` | Ключ API для сервиса VirusTotal (проверка ссылок/файлов). |
| `LEAKLOOKUP_PUBLIC_KEY` | Публичный ключ для сервиса LeakLookup (агрегатор утечек). |
| `AI_TUNNEL_TOKEN` | Токен для доступа к ai-tunnel, провайдера ИИ моделей. |

### Шаг 2: Запуск контейнеров

Выполните следующую команду в терминале, находясь в директории с файлами `docker-compose.yml` и `.env`:

```bash
docker compose up -d --build
```